version: "3.9"
services:
  web:
    build: .
    container_name: pocket-allowance-web
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
